node_modules
dist
dist-ssr
coverage
playwright-report
test-results

# Command Code session data (taste, agents, plans) — machine-local.
.commandcode/

*.local
.env
.env.local
.DS_Store
*.wasm.tmp
.wasm-cache

# Incremental build state. Machine-local and regenerated by `tsc -b`.
*.tsbuildinfo
*.config.d.ts

# Transpiled config output. The .ts files are the single source of truth;
# stray .js siblings silently take precedence in Vite's resolution order and
# caused the .ts configs to be ignored entirely.
# NOTE: eslint.config.js is genuine flat config and must NOT be ignored.
vite.config.js
vitest.config.js
playwright.config.js
