/**
 * Demo entry point — bundles the SDK so dapp.html can work as a static file.
 * Exposes `window.injectWalletProvider` and `window.EVENTS`.
 */
export { injectWalletProvider } from './index';
export { EVENTS } from './protocol';
